{
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "type-check": "pnpm -r type-check"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "ink-testing-library>react": "19",
        "ink-testing-library>ink": "6"
      }
    }
  }
}
